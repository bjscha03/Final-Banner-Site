import React from 'react';
export const AdminAIUploadPanel=({referenceImage,setReferenceImage}:any)=> <div className='border border-dashed border-zinc-600 p-4 text-center text-sm'>{referenceImage?<div><img src={referenceImage} className='w-full h-40 object-cover mb-2'/><button onClick={()=>setReferenceImage(null)} className='text-red-400'>Remove</button></div>:<label className='cursor-pointer'>Drag/drop or upload reference image<input type='file' accept='image/*' className='hidden' onChange={(e:any)=>{const f=e.target.files?.[0];if(!f)return;const r=new FileReader();r.onload=()=>setReferenceImage(String(r.result));r.readAsDataURL(f);}}/></label>}</div>;
