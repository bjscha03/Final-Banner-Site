name: Finish abandoned-cart Admin browser repair

on:
  push:
    branches:
      - feat/automatic-large-banner-25

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/automatic-large-banner-25
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
      - run: npm ci
      - name: Patch payload normalization and responsive test locator
        run: python scripts/patch-abandoned-cart-admin-browser.py
      - name: Run desktop and mobile Admin browser contracts
        run: |
          browser=$(command -v google-chrome || command -v google-chrome-stable || command -v chromium || command -v chromium-browser || true)
          test -n "$browser"
          PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH="$browser" npx playwright test --config=playwright.preview.config.ts admin-commerce-analytics.playwright.spec.ts --project=chromium-1440x900 --project=chromium-pixel8-portrait --workers=1
      - name: Lint repaired files
        run: npx eslint src/pages/admin/AbandonedCarts.tsx tests/browser/admin-commerce-analytics.playwright.spec.ts
      - name: Commit repair and remove temporary helpers
        run: |
          rm .github/workflows/diagnose-admin-commerce-browser.yml
          rm scripts/patch-abandoned-cart-admin-browser.py
          git config user.name "Banners On The Fly CI"
          git config user.email "actions@users.noreply.github.com"
          git add src/pages/admin/AbandonedCarts.tsx tests/browser/admin-commerce-analytics.playwright.spec.ts .github/workflows/diagnose-admin-commerce-browser.yml scripts/patch-abandoned-cart-admin-browser.py
          git commit -m "Harden abandoned-cart Admin browser flow"
          git push origin HEAD:feat/automatic-large-banner-25
